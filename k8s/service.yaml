apiVersion: v1
kind: Service
metadata:
  name: bank-api
  namespace: banking-system
  labels:
    app: bank-api
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: bank-api
