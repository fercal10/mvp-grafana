apiVersion: apps/v1
kind: Deployment
metadata:
  name: transfers-api
  namespace: banking-system
  labels:
    app: transfers-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transfers-api
  template:
    metadata:
      labels:
        app: transfers-api
    spec:
      containers:
      - name: transfers-api
        image: transfers-api:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8081
          name: http
        envFrom:
        - configMapRef:
            name: transfers-api-config
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data
          mountPath: /root/data
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: transfers-data-pvc
