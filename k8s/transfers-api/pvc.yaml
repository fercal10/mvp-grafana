# PVC para SQLite de transfers-api (ReadWriteOnce compatible con k3d/kind).
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: transfers-data-pvc
  namespace: banking-system
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
